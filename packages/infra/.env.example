# Copy this file to .env and fill in a real value for local development.
# .env is gitignored — never commit a real password.

# Encrypts/decrypts secret values Alchemy stores in .alchemy/ local state.
# Generate a strong one, e.g.: node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
ALCHEMY_PASSWORD=
